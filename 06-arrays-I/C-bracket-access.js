/* Access elements in an array the same way you'd access a character in a string: using brackets and the index number corresponding to the position of the element inside the array */

// Reminder, the first element in an array is at index 0, NOT index 1

let names = ['George', 'John', 'Thomas'];

console.log(names[0]);

console.log(names[1]);

console.log(names[2]);

console.log(names[3]);

/*
Notes:
--------

*/